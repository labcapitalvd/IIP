[project]
name = "shared_enums"
version = "0.1.0"
description = "package para configurar el shared_enums."
requires-python = ">=3.12"
dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_enums"]
