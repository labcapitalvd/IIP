[project]
name = "api"
version = "0.1.0"
description = "Contenedor encargado de publicar endpoints desde la base de datos"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.120.2",
    "uvicorn>=0.38.0",
]
